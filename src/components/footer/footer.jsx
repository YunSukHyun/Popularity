import styles from "./footer.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p className={styles.title}>Choose your favorites</p>
    </footer>
  );
};

export default Footer;
